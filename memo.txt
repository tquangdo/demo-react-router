Deploy Heroku just ONLY for json-server, NOT for React app
1/ package.json
 "dependencies": {
    "json-server": "^0.16.1",
 }
* HEROKU json-server:
"start": "node server.js",
(
* React app:
KO cần "start"!!! 

2/ ConfigConst.js
API_URL = (process.env.NODE_ENV === 'production') ? `https://${process.env.REACT_APP_DOMAINNAME}:${process.env.PORT}` : 'http://localhost:3000'

3/ server.js

4/ run cmd: $npm start
